// Root project - LibreShockwave SDK for reading Director/Shockwave files
plugins {
    id 'com.vanniktech.maven.publish' version '0.30.0' apply false
}

group = 'net.libreshockwave'
version = '0.1.0'

// Apply common settings to all subprojects
subprojects {
    group = rootProject.group
    version = rootProject.version
}

// Convenience tasks to run subproject tasks from root

tasks.register('runTests') {
    group = 'verification'
    description = 'Run SDK tests'
    dependsOn ':sdk:test'
}

tasks.register('buildAll') {
    group = 'build'
    description = 'Build all subprojects'
    dependsOn ':sdk:build'
}